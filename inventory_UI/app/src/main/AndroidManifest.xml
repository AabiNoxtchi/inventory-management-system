<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.inventoryui">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Models.AuthenticationManager"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.Test.Test2Activity"></activity>
        <activity android:name=".Activities.Test.TestActivity" >
        <intent-filter>
            <action android:name="android.intent.action.Main" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".Activities.Test.TestRecyclerApperance">
            <intent-filter>
                <action android:name="android.intent.action.Main" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.Employees.EmployeeAddActivity" />
        <activity android:name=".Activities.Employees.EmployeesMainActivity" />
        <activity android:name=".Activities.Products.ProductAddActivity" />
        <activity android:name=".Activities.Products.ProductsMainActivity">

            <!-- android:parentActivityName=".Controllers.Products.ProductsMainActivity"> -->

        </activity>
        <activity android:name=".Activities.Admin.AdminAddMolActivity" />
        <activity android:name=".Activities.Admin.AdminMainActivity" />
        <activity
            android:name=".Activities.MainActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.Main" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>