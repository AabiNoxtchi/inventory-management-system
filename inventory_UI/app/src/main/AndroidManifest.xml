<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.inventoryui">

    <uses-permission android:name="android.permission.INTERNET" />
    <!--<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />-->

    <application
        android:name=".Models.AuthenticationManager"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Controllers.Employees.EmployeeAddActivity"></activity>
        <activity android:name=".Controllers.Employees.EmployeesMainActivity" />
        <activity android:name=".Controllers.Products.ProductAddActivity" />
        <activity
            android:name=".Controllers.Products.ProductsMainActivity">
           <!-- android:parentActivityName=".Controllers.Products.ProductsMainActivity">-->

        </activity>
        <activity android:name=".Controllers.Admin.AdminAddMolActivity" />
        <activity android:name=".Controllers.Admin.AdminMainActivity" />
        <activity android:name=".Controllers.MainActivity"
                  android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.Main" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>